# service.yaml
apiVersion: v1
kind: Service
metadata:
  name: __APP_NAME__
  namespace: __NAMESPACE__
  labels:
    app: myapp
    costcenter: "dvsaidevops"
spec:
  type: ClusterIP
  selector:
    app: __APP_NAME__
  ports:
    - name: http
      port: 80
      targetPort: 8080
